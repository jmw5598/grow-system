export enum ApplicationContextKeys {
  ACTIONS = 'actions',
  CONFIG = 'config',
  NODES = 'nodes',
}

export enum ChannelSegments {
  COMMAND = 'command',
  COMPONENT = 'component',
  EVENT = 'event',
  INBOUND = 'inbound',
  NODE = 'node',
  REGISTER = 'register',
  SCHEDULER = 'scheduler',
  SCHEDULE = 'schedule',
  STATUS = 'status',
  SYSTEM = 'system',
}
