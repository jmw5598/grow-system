'use strict';

class ComponentAction {

  constructor(id, alias, type, pin) {
    this.id = id;
    this.alias = alias;
    this.type = type;
    this.pin = pin;
  }

}

module.exports = ComponentAction;
